// 日志展示的内容并非完整内容，仅展示关键部分
// v:MAJOR.MINOR.PATCH(V|M|DD(N))

--- Früh (Nur Hauptphasen), 2026.6.28

WANDAN - Kernregel 当前制作组人数: 1 (
    1. Muxian Ye
)

# v0.0.1628 - Zeile 809; 27,442 B
    1. 新项目 Kernregel，使用 C 语言解释型。

# v0.0.1628(~4) - Zeile 1924; 72,604 B
    1. 使用 C++ 重构。
    2. 引入 POSIX regex 正则，支持正则匹配、替换、分割等。
    3. 各种内置方法，长度、随机数等。

# v0.0.1628(4~7) - Zeile 2775; 106,577 B
    1. 新增功能:
        前置或后置递增递减;
        三元运算符短路求值;
        <MAX_RECURSION_DEPTH> 递归深度限制 1000;
        <MAX_LOOP_ITERATIONS> 非遍历循环迭代限制 1000000;
        精确的语法错误位置，<error_at> 打印文件名、行号、列号，并显示出错行及其标记;
        数组复合赋值等。

# v0.0.1705 - Zeile 7955; 335,727 B
    1. <MAX_RECURSION_DEPTH> 递归深度限制增加到 10000。
    2. <MAX_LOOP_ITERATIONS> 非遍历循环迭代限制增加到 10000000。

# v0.0.1709 - Zeile 10311; 437,761 B
    1. 新增功能:
        lies 可配合 mit 使用，支持各种功能，如正则、参数等。

# v0.0.1711 - Zeile 11274; 479,960 B
    1. 重构报错逻辑，新增 <KRError>，以及 <print_source_context> 报错上下文。
    2. 顶部声明版本号，<MAJOR> <MINOR> <PATCH>。

# v0.0.1712 - Zeile 11476; 480,827 B
    1. 新增功能:
        <KDDataSet> 对 KD 文件的操作，包括写入、加密、解密等。

# v0.0.1713 - Zeile 12947; 548,386 B
    1. 新增功能:
        <http_anfrage> 各种 HTTP 操作，含异步。

# v0.0.1722 - Zeile 17009; 719,027 B
    1. 扩展了各种三角函数、微积分的内置函数。

# v0.0.1723 - Zeile 17211; 727,788 B
    1. 新增功能:
        缩进模式，无需花括号即可解析块;
        lexikal 别名解析等。

--- 2026.8

# v0.0.1730 - Zeile 19879; 851,365 B
    1. 修复若干BUG。
    2. 新增功能:
        <suggest_correction> 未定义变量、函数名、类名，或错误的关键字，报错时将模糊匹配，建议为可能的正确名称。

# v0.0.1802 - Zeile 20607; 887,404 B
    1. 修复函数值复制部分缺失的BUG。
    2. 优化功能:
        <suggest_correction> 优化了模糊匹配逻辑;
        赋值改为表达式而非语句。
    3. 新增功能:
        类方法内支持更多语句;
        变量复制或初始化可带类型注解。

# v0.0.1803 - Zeile 20737; 893,651 B
    1. 将对象行为改为非引号直接作为变量求值。
    2. 修复函数体中枚举双重释放的BUG。
    3. 优化功能:
            ver 可以捕获内部异常(但仍无法捕获语法错误);
            范围表达式采用智能步长，智能判断是否为负步长。

# v0.0.1804 - Zeile 21091; 911,121 B
    1. 修复导入文件时报错行号及文件名不正确的问题。
    2. 新增命令: 自定义报错上下文行数。

# v0.0.1809 - Zeile 26354; 1,171,487 B
    1. 修复若干BUG。
    2. 新增功能:
        新增 AST 宏和预处理宏。
        新增 satz 函数修饰符，允许创建语句函数。

# v0.0.1811 - Zeile 28636; 1,283,768 B
    1. 新增功能:
        重构类型别名体系，新增结构体、联合体;
        添加 ast 内置函数和打印 AST 的启动选项。

# v0.0.1812 - Zeile 28666; 1,285,302 B
    1. 新增子语言 verwirrt，可直接与主语言交互，该语言实现与主语言不在同一文件内，因此不计入行。
    2. 完善 verwirrt 子语言的嵌入式语义高亮。
    
# v0.0.1813 - Zeile 29410; 1,317,236 B
    -

# v0.0.1823 - Zeile 30193; 1,354,462 B
    1. 修复赋值取值不完善的问题。
    2. 修复私有类访问权限的问题。

# v0.0.1824 - Zeile 30488; 1,367,847 B
    1. 新增 VA 汇编语言的内嵌连接(外部编译器，但也为 WANDAN 作品)。
    2. 完善 VA 汇编语言的嵌入式语义高亮。

# v0.0.1911 - Zeile 35424; 1,599,203 B
    1. 优化了报错上下文的稳定性。
    2. 添加 --lex 命令，及优化了 ast 命令，用作 TypeScript 语义高亮读取信息:
        解析阶段处理函数、变量、类、类型等定义的存在，输出标准报错输出;
        AST 节点返回信息更加丰富，TypeScript 端将分析所有变量、函数、参数、类、结构体、联合体、枚举体、枚举成员、类成员的属性和状态，以及整数、浮点数常量(支持所有进制)的属性;
        解决了预处理宏的可用性问题，并增加 #snfa 分支，#r 通用删除，优化了 Lexikal 和预处理指令的时机;
        为 #fa 等条件指令添加了各种比较运算符;
        添加了 28 种系统宏，可以区分系统、环境、版本等，可用作预处理命令的比较或直接在主代码中使用其值;
        修复了宏名标识符过严格的问题，修复了宏替换没有跳过字符串和注释的问题。
    3. 优化了 klass:
        同类内静态成员，函数和变量都可以直接访问;
        可以省略类内函数声明的前缀，可以直接写修饰符或仅函数名。
    4. 优化了类型:
        修复结构体花括号多余消耗的问题;
        使用自定义类型前缀直接声明变量时会自动构造，而不是默认 null，同时修复 null 不检测类型是否符合的问题。
    5. 循环体的 weiter 和 bruch 可以指定跳出层级，应对嵌套循环。

# v0.0.1912 - Zeile 36424; 1,641,417 B
    1. 若干优化及 BUG 修复。
    2. 添加指针操作以及指针类型。
    3. 优化变量赋值的值拷贝，优化循环执行效率，优化范围迭代求值逻辑，裸循环体百万次迭代可达 7 ms (优化前大约 800 ms)，加上递增表达式可达 20 ms。
    4. 优化了各种最大迭代次数。

# v0.0.1915 - Zeile 39779; 1,798,030 B
    1. 若干优化及 BUG 修复。
    2. 添加对对象复合赋值的功能。
    3. 优化 HTTP 请求，添加多种内置函数用于 HTTP。
    4. 添加若干字符串、数组、对象方法。
    5. 修复函数类型表达式歧义的问题，优化了获取类型的内置函数及方法。

# v0.0.1923 - Zeile 54567; 2,467,711 B
    1. 将主执行模式改为编译后 VM 执行，所有节点、内置函数、内置方法均完成迁移，无解释器复用，但保留解释器以确保稳定。
    2. 若干优化。